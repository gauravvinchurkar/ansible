global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100'] ##specify the hostname or ip for prometheus
  # Add additional scrape jobs as needed

rule_files:
  - /etc/prometheus/alert.rules
